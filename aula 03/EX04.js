let a = 20
let b = 15
let c = 9

if (a <= b && a <= c && b <= c ) {
    console.log(`${a} - ${b} - ${c}`)
        } else {

    if (a <= b && a <= c && b <= c ) {
    console.log(`${a} - ${c} - ${c}`)
    } else {

        if (a <= b && a <= c && b <= c ) {
          console.log(`${b} - ${a} - ${ac}`) 
        } else {

            if (a <= b && a <= c && b <= c ) {
    console.log(`${b} - ${c} - ${ca}`)
            } else{

                if (a <= b && a <= c && b <= c ) {
    console.log(`${c} - ${a} - ${b}`)
                } else {
                    console.log(`${c} - ${b} - ${a}`)
            }
        }
    }
}
        }